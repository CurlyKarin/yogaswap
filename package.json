{
  "scripts": {
    "test": "npm run test --prefix app && npm run test --prefix backend",
    "lint": "npm run lint --prefix app"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.888.0",
    "axios": "^1.12.2",
    "tofu": "^0.0.3"
  },
  "devDependencies": {
    "@types/aws-sdk": "^0.0.42",
    "@types/node": "^24.4.0",
    "typescript": "^5.9.2"
  }
}
