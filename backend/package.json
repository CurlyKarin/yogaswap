{
  "name": "yogaswap-backend",
  "version": "1.0.0",
  "description": "Serverless backend für YogaSwap Demo",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "seed": "ts-node src/seed/index.ts",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "zip": "npm run build && node scripts/zipLambdas.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.890.0",
    "@aws-sdk/client-lambda": "^3.388.0",
    "@aws-sdk/lib-dynamodb": "^3.388.0",
    "@yogaswap/shared": "file:../shared",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.4.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2",
    "archiver": "^5.3.2"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  }
}
